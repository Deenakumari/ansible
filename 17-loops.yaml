- name: ansible loops
  hosts: local
  connection: local
  tasks:
   - name: print the names
     ansible.builtin.debug:
      msg: "hello print {{ item }}" #here item is reserved keyword
     loop:
     - deena
     - kumari
     - madhumanchi
