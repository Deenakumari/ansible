- name: variables from inventory
  hosts: frontend
  tasks:
   - name: print the variable info
     ansible.builtin.debug:
      msg: 'course info course is: "{{ course }}", name is: "{{ trainer }}", duration is: "{{ duration }}" '