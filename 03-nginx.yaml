- name: nginx install and start
  hosts: frontend
  become: yes
  tasks:
     - name: nginx is installed
       ansible.builtin.package:
       name: nginx
       state: present

     - name: start nginx
       ansible.builtin.service:
       name: nginx
       state: started
       enabled: yes  


